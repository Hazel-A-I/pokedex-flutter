name: pokedex_flutter
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  http: ^1.1.0
  flutter:
    sdk: flutter
  rxdart: ^0.27.7
  flutter_bloc: ^8.1.3
  mobx: ^2.2.0
  flutter_mobx: ^2.1.0
  provider: ^6.0.5
  graphql_flutter: ^5.1.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.6
  mobx_codegen: ^2.4.0

flutter:
  uses-material-design: true
